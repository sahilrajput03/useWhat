{
	"name": "usewhat",
	"version": "0.0.4",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "c && yarn version --patch && p",
		"start1": "c && yarn version --minor && p",
		"start2": "c && yarn version --major && p",
		"test": "echo \"Error: no test specified.\" && exit 1"
	},
	"dependencies": {
		"react": "*"
	},
	"keywords": [],
	"author": "",
	"license": "ISC"
}
